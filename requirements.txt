# Eye Cataract Detection — dependencies
# Python >= 3.10 recommended

# Method 1 — HOG + SVM notebook (CPU-friendly)
opencv-python>=4.8
numpy>=1.24
scikit-learn>=1.3
scikit-image>=0.21
matplotlib>=3.7
joblib>=1.3

# Method 2 — YOLOv5 notebook (GPU runtime recommended, e.g. Kaggle T4 / Colab)
# The notebook clones ultralytics/yolov5 and installs its own requirements,
# so nothing to pin here — but for local runs:
#   git clone https://github.com/ultralytics/yolov5 && pip install -r yolov5/requirements.txt

# Dataset download
roboflow>=1.1
