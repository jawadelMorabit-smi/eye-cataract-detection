# Dependencies for the cataract detection mini-project
# Python >= 3.10

opencv-python>=4.8
numpy>=1.24
matplotlib>=3.7

# Method 1 — HOG + SVM (CPU-friendly)
scikit-learn>=1.3        # StandardScaler, LinearSVC, metrics
scikit-image>=0.21       # HOG descriptor visualization
joblib>=1.3              # model saving

# Method 2 — YOLOv5: run on Kaggle/Colab GPU; the notebook clones
# https://github.com/ultralytics/yolov5 and installs its own requirements.txt.
# Also needed there:
roboflow                 # dataset download (use Kaggle Secrets for the API key)
